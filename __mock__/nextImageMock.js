import React from 'react';

const NextImageMock = ({ src, alt, width, height, style }) => (
  <img src={src} alt={alt} width={width} height={height} style={style} />
);

export default NextImageMock;
